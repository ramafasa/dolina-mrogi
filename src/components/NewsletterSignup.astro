---
---

<section id="newsletter" class="py-16 sm:py-24">
  <div class="mx-auto max-w-xl px-4">
    <div class="rounded-3xl bg-forest p-8 text-center sm:p-12">
      <h2 class="font-serif text-3xl font-bold text-cream sm:text-4xl">
        Odbierz -25% na start
      </h2>
      <p class="mt-3 text-cream/80">
        Zapisz się na listę oczekujących i&nbsp;jako pierwszy/a dowiedz się o&nbsp;starcie zamówień. Kod rabatowy wyślemy mailem.
      </p>

      <!-- Form -->
      <form id="newsletter-form" class="mt-8 space-y-4 text-left">
        <div>
          <label for="email" class="block text-sm font-medium text-cream/80">Adres e-mail *</label>
          <input
            type="email"
            id="email"
            name="email"
            required
            placeholder="jan@example.com"
            class="mt-1 w-full rounded-xl border-0 bg-white/10 px-4 py-3 text-cream placeholder:text-cream/40 focus:outline-none focus:ring-2 focus:ring-honey"
          />
        </div>

        <div class="flex items-start gap-2">
          <input
            type="checkbox"
            id="rodo"
            name="rodo"
            required
            class="mt-1 h-4 w-4 rounded border-cream/30 bg-white/10 text-honey focus:ring-honey"
          />
          <label for="rodo" class="text-xs leading-relaxed text-cream/60">
            Wyrażam zgodę na przetwarzanie moich danych osobowych w celu otrzymywania informacji handlowych.
            <a href="/polityka-prywatnosci" class="underline hover:text-cream/80">Polityka prywatności</a>.
          </label>
        </div>

        <button
          type="submit"
          class="w-full rounded-xl bg-honey py-4 font-bold text-warm-brown transition hover:bg-honey/90 disabled:opacity-50"
        >
          Chcę -25% i info o starcie
        </button>
      </form>

      <!-- States -->
      <div id="form-loading" class="mt-6 hidden text-cream/80">
        <svg class="mx-auto h-6 w-6 animate-spin" fill="none" viewBox="0 0 24 24">
          <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4" />
          <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4z" />
        </svg>
        <p class="mt-2 text-sm">Zapisuję...</p>
      </div>

      <div id="form-success" class="mt-6 hidden rounded-xl bg-honey/20 p-4 text-cream">
        <p class="font-semibold">Dzięki! Sprawdź skrzynkę e-mail 📬</p>
        <p class="mt-1 text-sm text-cream/70">Kod START25 jest już w drodze do Ciebie.</p>
      </div>

      <div id="form-error" class="mt-6 hidden rounded-xl bg-red-500/20 p-4 text-cream">
        <p class="font-semibold">Ups, coś poszło nie tak.</p>
        <p class="mt-1 text-sm text-cream/70">Spróbuj ponownie za chwilę lub napisz do nas.</p>
      </div>

      <p class="mt-6 text-xs text-cream/40">
        Zero spamu. Wypisz się w dowolnym momencie. Dane przetwarzane przez MailerLite.
      </p>
    </div>
  </div>
</section>

<script>
  const form = document.getElementById('newsletter-form') as HTMLFormElement;
  const loading = document.getElementById('form-loading')!;
  const success = document.getElementById('form-success')!;
  const error = document.getElementById('form-error')!;

  form.addEventListener('submit', async (e) => {
    e.preventDefault();

    const email = (form.querySelector('#email') as HTMLInputElement).value;

    form.classList.add('hidden');
    loading.classList.remove('hidden');
    error.classList.add('hidden');

    try {
      const res = await fetch('/api/subscribe', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ email }),
      });

      if (res.ok) {
        loading.classList.add('hidden');
        success.classList.remove('hidden');
      } else {
        throw new Error('API error');
      }
    } catch {
      loading.classList.add('hidden');
      form.classList.remove('hidden');
      error.classList.remove('hidden');
    }
  });
</script>
